import bootstrap.Initialization;
import handler.CLI;

public class Main {
    public static void main(String[] args) throws Exception {
        Initialization bootstrapInstance = new Initialization();
        CLI cli = new CLI();
    }
}
